age_count = 1; // start the age counter
age = 23;
while(age_count < Math.sqrt(age) ) {
    console.log(`age ${age_count}`);
    age_count++;
}
console.log(`The square root of Dan's age is ${age_count} years old`);